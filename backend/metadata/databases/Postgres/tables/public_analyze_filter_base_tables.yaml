table:
  name: analyze_filter_base_tables
  schema: public
is_enum: true
array_relationships:
  - name: analyze_filters
    using:
      foreign_key_constraint_on:
        column: base_table
        table:
          name: analyze_filters
          schema: public
