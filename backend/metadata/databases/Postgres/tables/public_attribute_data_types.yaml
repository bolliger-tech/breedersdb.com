table:
  name: attribute_data_types
  schema: public
is_enum: true
array_relationships:
  - name: attributes
    using:
      foreign_key_constraint_on:
        column: data_type
        table:
          name: attributes
          schema: public
  - name: cached_attributions
    using:
      foreign_key_constraint_on:
        column: data_type
        table:
          name: cached_attributions
          schema: public
