{
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{js,ts,vue,scss,html,md,json,yaml,yml}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.2"
  },
  "lint-staged": {
    "**/*.{js,ts,vue,scss,html,md,json,yaml,yml}": "prettier --write --ignore-unknown"
  }
}
